import LoginForm from "../components/auth/LoginForm";

const Login = () => {
  return (
    <main className="bg-[#FAFAFA] flex items-center min-h-screen justify-center">
      <LoginForm />
    </main>
  );
};

export default Login;
